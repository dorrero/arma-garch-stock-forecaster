# Direct dependencies only - transitive deps (certifi, urllib3, patsy, etc.)
# are resolved automatically by pip. Versions below were verified together
# against Python 3.11.
Django==5.2.17
requests==2.32.3
yfinance==1.5.2
numpy==2.4.6
pandas==3.0.5
statsmodels==0.14.6
arch==8.0.0
matplotlib==3.11.1
